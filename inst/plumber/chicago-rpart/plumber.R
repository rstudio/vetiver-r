# Generated by the vetiver package; edit with care

library(pins)
library(plumber)
library(rapidoc)
library(vetiver)

# Packages needed to generate model predictions
if (FALSE) {
    library(dials)
    library(dplyr)
    library(parsnip)
    library(purrr)
    library(recipes)
    library(rlang)
    library(rpart)
    library(rsample)
    library(tibble)
    library(tidyr)
    library(tune)
    library(vctrs)
    library(workflows)
    library(yardstick)
}
b <- board_rsconnect("envvar", server = "https://colorado.rstudio.com/rsc")
v <- vetiver_pin_read(b, "julia.silge/chicago_ridership", version = "53335")

#* @plumber
function(pr) {
    pr %>% vetiver_api(v, debug = TRUE)
}
